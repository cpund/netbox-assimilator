from setuptools import find_packages, setup

setup(
    name='netbox-assimilator',
    version='0.1',
    description='We will assimilate all of your devices into Netbox',
    url='https://github.com/cpund/netbox-assimilator',
    author='Craig Pund',
    license='Apache 2.0',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
